#pragma once

namespace sq::gui {

/** @brief Portable key name used by the GUI input boundary. */
enum class Key {
    left,
    right,
    up,
    down,
    home,
    end,
    backspace,
    delete_key,
    enter,
    space,
    tab,
    escape
};

} // namespace sq::gui
